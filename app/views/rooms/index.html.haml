%h1 Rooms

%br  
%h5 You can search by room's number or proctor's name

= form_tag rooms_path, method: 'get' do
  = text_field_tag :search, params[:search]
  = submit_tag "Search", :name => nil


- if @rooms.each do |room| 
  - # code or partial to display the message 
- end.empty? 
  %br
  %h7 There is no room stored on database
  
  
%div{class:'media'}
  
  %div{class:"media-body"}

%table{class:"table table-hover"}
  %tr
    %th Room Number
    %th Building
    %th Date
    %th Time
    %th Proctor 1
    %th Proctor 2
    %th
    %th
    %th

  - @rooms.each do |room|
    %tr
      %td= room.number
      %td= room.build
      %td= room.date
      %td= room.time
      %td= room.proctor1
      %td= room.proctor2
      %td= link_to 'Show', room
      %td= link_to 'Edit', edit_room_path(room)
     

%br


= link_to 'Add Room', new_room_path

